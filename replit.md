# AutoTube Studio Pro

Automated YouTube video generation system.

## Setup
1. Add secrets: `GEMINI_API_KEY`, `PEXELS_API_KEY`, `ELEVENLABS_API_KEY`, `YOUTUBE_CLIENT_SECRET`.
2. Authorize YouTube via the app UI.
3. Start the scheduler in the UI.
